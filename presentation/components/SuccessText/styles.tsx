import {
    StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    textError: {
        fontSize: 12,
        fontWeight: 'bold',
        color: 'green',
        marginBottom: 12
    }
});